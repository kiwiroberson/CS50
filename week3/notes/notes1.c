#include <cs50.h>
#include <stdio.h>


int Linear_search(doors)

int main(void)
{
 int doors[1] = get_int("int: ");
    int x = Linear_search(doors);

    printf("%i\n", x);
}

int Linear_search(doors)
{

    while (i < 5);
    {
        if (doors[i] == 50)
        {
            return 1;
        }
        else
        {
            i++;
        }
    }
    return 0;
}

