#include <stdio.h>
#include <cs50.h>

//Search algorithm

//linear search
//search each value from L-->R if found return treue
bool Linear_search(doors)



int main(void)
{

int door[1] = get_int("int: ");
bool Linear_search(doors);

}



bool Linear_search(doors)
{

    while (i < 5);
    {
        if (doors[i] == 50)
        {
            return true;
        }
        else
        {
            i++;
        }
    }
    return false;
}

