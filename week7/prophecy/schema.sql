CREATE TABLE students (
    id INTEGER,
    student_name TEXT,
    PRIMARY KEY(id)
);

CREATE TABLE house (
    id INTEGER,
    house_name TEXT,
    
)

CREATE TABLE link (
    student_id INTEGER,
    house_id INTEGER,
)